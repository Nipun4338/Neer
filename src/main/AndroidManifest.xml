<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nogor">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".edit_my_ad_Activity"></activity>
        <activity android:name=".all_ads_Activity" />
        <activity android:name=".forgotten_change_passwordActivity" />
        <activity android:name=".forgotten_verifyActivity" />
        <activity android:name=".forgotten_passwordActivity" />
        <activity android:name=".my_ads_Activity" />
        <activity android:name=".post_an_add_2Activity" />
        <activity android:name=".post_an_addActivity" />
        <activity android:name=".look_for_officeActivity" />
        <activity android:name=".look_for_houseActivity" />
        <activity android:name=".updateprofileActivity" />
        <activity android:name=".aboutusActivity" />
        <activity android:name=".registerActivity" />
        <activity android:name=".verifyActivity" />
        <activity android:name=".profileActivity" />
        <activity android:name=".loginActivity" />
        <activity android:name=".signActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>